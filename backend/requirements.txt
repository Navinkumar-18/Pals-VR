# Ambedkar Digital Heritage API — Python dependencies
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30
pydantic>=2.7
pydantic-settings>=2.3
psycopg[binary]>=3.2
pytest>=8.2
httpx>=0.27
# --- Phase E: OCR document ingestion ---
# multipart uploads for FastAPI need python-multipart
python-multipart>=0.0.9
# PDF page rendering (pure-pip wheels; no poppler needed)
pymupdf>=1.24
# Default OCR engine (ONNX models bundled in the wheel - offline, English)
rapidocr-onnxruntime>=1.2.3
# Image handling used by engine/PDF rendering and validation
pillow>=10.0